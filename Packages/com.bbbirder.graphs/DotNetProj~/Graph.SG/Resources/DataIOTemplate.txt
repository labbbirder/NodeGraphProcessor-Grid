// NOTE: THIS IS AN AUTO-GENERATED FILE, NEVER MODIFY IT!
// Generated by UnityVue
// Author: bbbirder (labbbirder)
// Assembly: {{module_name}}
using System;
using System.Collections.Generic;
using System.Reflection;
using UnityEngine.Scripting;
using GraphProcessor;

{{ (target_namespace | string.empty) ? "" : "namespace "+target_namespace+"{" }}

partial class {{node_name}} {
	{{~ for f in output_fields ~}}
	[Preserve]
	private static T get_{{f.name}}<T>(BaseNode self)
	{
        {{~ if f.has_accessor_method ~}}
	    return ConvertData<{{f.type}}, T>((self as {{node_name}}).get_{{f.name}}());
		{{~ else ~}}
	    return ConvertData<{{f.type}}, T>((self as {{node_name}}).@{{f.name}});
	    {{~ end ~}}
	}
	{{~ end ~}}

	{{~ for f in input_fields ~}}
	[Preserve]
	private static void set_{{f.name}}(BaseNode self, {{f.type}} value)
	{
        {{~ if f.has_accessor_method ~}}
        ; (self as {{node_name}}).set_{{f.name}}(value);
		{{~ else ~}}
		; (self as {{node_name}}).@{{f.name}} = value;
	    {{~ end ~}}
	}
	{{~ end ~}}
}

{{ (target_namespace | string.empty) ? "" : "}" }}
